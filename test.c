#include <stdio.h>
#include <math.h>

float deg2rad(float degrees)
{
    return degrees * (M_PI * 180.0);
}   

int main()
{
    float hypotensue;
    float opposite = 160;
    float adjacent;
    float degree = 60;

    printf("%f\n", 60 * (M_PI / 180) / 1472);
}